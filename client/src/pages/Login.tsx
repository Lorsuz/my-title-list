import Layout from '@layout/PagesLayout';

const Login = () => {
	// !Saber se user esta logado, se estiver logado n deixar vir pra esta pagina
	// !tratar erros no front com zod schema
	// !usar logica para de formulario, hook useForm
	// !madnar os dados para a respectiva api
	
	return <Layout title='Formulario de Authenticação'>ariel</Layout>;
};

export default Login;
